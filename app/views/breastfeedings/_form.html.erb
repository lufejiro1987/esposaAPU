<%= form_with(model: breastfeeding, local: true) do |form| %>
  <% if breastfeeding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(breastfeeding.errors.count, "error") %> prohibited this breastfeeding from being saved:</h2>

      <ul>
        <% breastfeeding.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :baby_id %>
    <%= form.select :baby_id, options_from_collection_for_select(@babies, 'id', 'name', breastfeeding.baby_id) %>
  </div>

  <div class="field">
    <%= form.label :time, 'Time (in minutes)' %>
    <%= form.number_field :time %>
  </div>

  <div class="field">
    <%= form.label :quantity, 'Quantity (in milliliters)' %>
    <%= form.number_field :quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
